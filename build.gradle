apply plugin:'application'
apply plugin:'scala'

apply plugin: 'idea'

repositories {
    mavenCentral()
}

mainClassName = "cm.app.catf.Main"

dependencies {
    compile 'org.scala-lang:scala-library:2.10.3'

    compile 'junit:junit:3.8.1'
}